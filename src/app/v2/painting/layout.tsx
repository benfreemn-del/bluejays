export const metadata = {
  title: "[V2] Transform Every Surface | Premium Painting Template",
  description:
    "V2 premium animated painting contractor template with vibrant purple, paint drop particles, and before/after gallery.",
};

export default function V2PaintingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
