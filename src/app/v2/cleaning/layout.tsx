export const metadata = {
  title: "[V2] Sparkling Clean Spaces | Premium Cleaning Template",
  description:
    "V2 premium animated cleaning service template with sparkle particles, glass morphism, and fresh micro-interactions.",
};

export default function V2CleaningLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
