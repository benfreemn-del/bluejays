export const metadata = {
  title: "[V2] Fine Dining Experience | Premium Restaurant Template",
  description:
    "V2 premium animated restaurant template with rich gallery, ember particles, and warm culinary ambiance.",
};

export default function V2RestaurantLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
