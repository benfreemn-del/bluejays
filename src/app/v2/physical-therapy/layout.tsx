export const metadata = {
  title: "[V2] Get Back to Life | Premium Physical Therapy Template",
  description:
    "V2 premium animated physical therapy template with healing blue + warm green design.",
};

export default function V2PhysicalTherapyLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
