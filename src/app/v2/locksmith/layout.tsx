export const metadata = {
  title: "[V2] SecureKey Locksmith | Premium Locksmith Template",
  description:
    "V2 premium animated locksmith template with gold accents, key animations, and security-focused micro-interactions.",
};

export default function V2LocksmithLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
