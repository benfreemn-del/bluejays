export const metadata = {
  title: "[V2] Rapid Response Towing | Premium Towing Template",
  description:
    "V2 premium animated towing template with emergency-focused design, urgency elements, and rescue micro-interactions.",
};

export default function V2TowingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
